IMPORTANT!

If you are looking for our advanced fork of Jenni, an extendible IRC bot, you are in the wrong place.

The fork was renamed to Willie, because it drifted away from the original jenni. Therefor, you are looking for https://github.com/embolalia/willie

If you have this repo already cloned on your machine, no need to re-clone. You can change the origin URL using the following command:

(For push access, existing contributors only)
git remote set-url origin git@github.com:embolalia/willie.git

(For pull only access using the git protocol)
git remote set-url origin git://github.com/embolalia/willie.git

(For pull only access using the https protocol)
git remote set-url origin https://github.com/embolalia/willie.git
